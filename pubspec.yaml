
name: flutter_shop
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # ✅ Firebase — Upgrade ແກ້ Bug AdminMainScreen
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  firebase_storage: ^13.3.0
  firebase_messaging: ^16.2.0

  # ✅ Notifications
  flutter_local_notifications: ^17.2.3

  # ✅ UI
  cupertino_icons: ^1.0.8
  get: ^4.6.6
  velocity_x: ^4.1.3
  cached_network_image: ^3.4.1

  # ✅ Image
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
